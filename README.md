# Setup

1. `npm install`
2. `npm run dev`
3. Open browser: `http://localhost:6969`

# notes

- do we put the entities in/on tiles?  ramifications for large bulk?
- when pointer leaves grid, we need to dismiss hover on any hovered tiles, if need be