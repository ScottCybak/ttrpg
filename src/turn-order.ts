export class TurnOrder {
    // we need all npc/players to be in the available pool
    // dm option - 'Set turn order'
    
    // this popups up a dialog, that lets us set a numeric value
    // for each player/npc.  it also allows us to make certain
    // npc's 'hidden' from the turn order.

}